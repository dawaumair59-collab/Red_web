kind = "web"
previewPath = "/"
title = "Restaurant Order Management"
version = "1.0.0"
id = "artifacts/restaurant-orders"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23395

[services.development]
run = "pnpm --filter @workspace/restaurant-orders run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/restaurant-orders", "run", "build" ]
publicDir = "artifacts/restaurant-orders/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23395"
BASE_PATH = "/"
